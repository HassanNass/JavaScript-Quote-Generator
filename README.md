# JavaScript-Quote-Generator

This is a small web project that generates and displays a random quote when a button is clicked. It showcases fundamental concepts like DOM manipulation, event listeners, and dynamic content updates using JavaScript.